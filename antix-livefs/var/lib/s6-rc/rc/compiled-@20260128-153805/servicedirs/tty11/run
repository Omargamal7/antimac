#!/usr/bin/execlineb -P

envfile /etc/s6-rc/config/tty11.conf
importas -uD "yes" SPAWN SPAWN
importas -sCuD "" ARGS ARGS
importas -sCuD "/sbin/agetty" GETTY GETTY

if -t { test -e /dev/tty11 }
if -t { test ${SPAWN} = "yes" }
	exec ${GETTY} ${ARGS}
