#!/usr/bin/execlineb -P

backtick -n USER { whoami }
importas -S USER
backtick -n HOME { homeof ${USER} }
importas -S HOME
elglob -0 scripts ${HOME}/.s6-rc/scripts/rc-local/*.stop
forx -E script { ${scripts} }
sh -c "${script}" 
